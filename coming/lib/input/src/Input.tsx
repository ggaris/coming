import React from 'react'

/**
 * 需要支持 
 * 数字输入框：支持浮点数，并支持设置最大位数，
 * 文本输入框：支持设置最大长度，
 * 密码输入框：支持设置正则，
 * 前缀输入框：支持自定义（不需要支持emoji）
 * @returns 
 */


const Input = () => {
  return (
    <input />
  )
}

export default Input
